namespace ZGConnect
{
    /// <summary>Facade vs roof slot for shared material lookup.</summary>
    public enum BuildingSurfaceMaterialType
    {
        Facade     = 0,
        RoofFlat   = 1,
        RoofSloped = 2,
    }
}

